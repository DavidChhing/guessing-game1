var name = "david chhing";

function capitalize(text {
    for(var character = 0; character<name.length; character++){
        var currentLetter = name.substring(character,character+1);
        var previousLetter = name.substring(character-1, character);
       
    //     if (previousLetter == ""){
    //         console.log (currentLetter.toUpperCase());
    //     } else if { (character ==0)
    
    //     {
    //     console.log (currentLetter);
    // }
    
    if (previousLetter == "") || (character == 0)) {
    
    {
        console.log (currentLetter.toUpperCase());
    } else if { (character ==0)
    
    {
    console.log (currentLetter);
    }



